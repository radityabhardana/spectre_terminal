{
  "name": "spectre-terminal",
  "version": "0.1.0",
  "description": "Local read-only Polymarket intelligence terminal with deterministic Dynamic EV guardrails.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/start-all.js",
    "web": "node src/web.js",
    "check": "node --check src/*.js && node --check public/app.js && node --check public/market-summary.js && node --check public/terminal-shell.js && node --check e2e/*.js && node --check playwright.config.js",
    "test:e2e": "playwright test",
    "search": "node src/cli-search.js",
    "test": "node --test test/*.test.js",
    "extract-tokens": "node scripts/extract_tokens.js",
    "backfill-tokens": "node scripts/backfill_tokens.js",
    "check-ids": "node scripts/check_ids.js"
  },
  "engines": {
    "node": ">=20.10"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "ethers": "^6.17.0",
    "ws": "^8.21.0"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "htmlhint": "^1.9.2"
  }
}
